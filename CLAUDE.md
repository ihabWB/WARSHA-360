## gstack

Use /browse from gstack for all web browsing. Never use mcp__claude-in-chrome__* tools.

Available skills: /office-hours, /plan-ceo-review, /plan-eng-review, /plan-design-review,
/design-consultation, /design-shotgun, /design-html, /review, /ship, /land-and-deploy,
/canary, /benchmark, /browse, /qa, /qa-only, /design-review, /setup-deploy, /retro,
/investigate, /document-release, /document-generate, /codex, /cso, /autoplan,
/careful, /freeze, /guard, /unfreeze, /gstack-upgrade, /learn.

## Project

<!-- يتم تعبئة هذا القسم لاحقاً: وصف المشروع، التقنيات، المسارات المهمة -->

## Rules

- عدّل الملفات جزئياً، لا تعيد كتابتها بالكامل
- لا تكتب أي كلمات سر أو مفاتيح داخل الكود — استخدم ملف .env
- اشرح أي تغيير على بنية قاعدة البيانات قبل تنفيذه
